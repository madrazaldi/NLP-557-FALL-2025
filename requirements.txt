# Core ML deps
numpy
pandas
scikit-learn
iterative-stratification

# Hugging Face ecosystem
transformers
accelerate
safetensors
tokenizers
huggingface-hub
tqdm
filelock
packaging
requests

# Optional: needed for some models (e.g., deberta-v3, many multilingual)
sentencepiece

# PyTorch (CPU build by default; pick a specific version if you need CUDA)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Serving / API
fastapi
uvicorn[standard]
jinja2
